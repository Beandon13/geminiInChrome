# Required: Your Gemini API key from https://aistudio.google.com/apikey
GEMINI_API_KEY=your_api_key_here

# Optional: Model to use (default: gemini-2.0-flash)
# Options: gemini-2.0-flash, gemini-2.0-pro, gemini-1.5-pro, gemini-1.5-flash
GEMINI_MODEL=gemini-2.0-flash

# Optional: Chrome DevTools Protocol host and port (default: localhost:9222)
CDP_HOST=localhost
CDP_PORT=9222

# Optional: Number of recent conversation turns to send to model (default: 50)
CONTEXT_TURNS=50

# Optional: Directory for session logs (default: ./sessions)
SESSIONS_DIR=./sessions
